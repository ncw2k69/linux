# Base
```
sudo apt install htop iotop ncdu mc net-tools curl neofetch sshfs lm-sensors 
```
# Ubuntu
```
sudo apt install htop iotop ncdu mc net-tools curl neofetch sshfs
```

# Debian
```
sudo apt install libfuse2
```
### Tools
```
sudo apt install vlc gdebi sshfs synaptic htop iotop ncdu cbm mc net-tools curl fastfetch rsync
```
### Yaru
```
sudo apt install yaru-theme-*
```
### Sensors
```
sudo apt install fancontrol i2c-tools read-edid lm-sensors 
```
### Mate
```
sudo apt install mate-desktop-environment* mate-icon-theme mate-indicator-applet* mate-media* mate-menu* mate-netbook mate-netbook* mate-notification-daemon* mate-panel* mate-power-manager* mate-sensors-applet* mate-session-manager mate-settings-daemon mate-system-monitor* mate-terminal* mate-themes mate-tweak mate-utils*
```
### Gnome
```
sudo apt install gnome-disk-utility gnome-extra-icons gnome-backgrounds gnome-dust-icon-theme gnome-human-icon-theme gnome-icon-theme* gnome-theme* gnome-weather gnome-wi*-icon*
```
